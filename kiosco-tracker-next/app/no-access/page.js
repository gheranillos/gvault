export default function NoAccessPage() {
  return (
    <div className="auth-wrap">
      <div className="auth-title">Acceso restringido</div>
      <div className="auth-sub">
        Tu acceso no esta habilitado. Contacta al administrador.
      </div>
    </div>
  );
}
